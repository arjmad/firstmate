# Copy to local, gitignored config/wedge-alarm and edit.
# docs/configuration.md "Away-mode wedge alarm channels" owns the schema.
# This example combines a macOS banner with a phone push through ntfy.sh.
osascript
command: curl -fsS -H "Title: firstmate wedge alarm" -d "$1" https://ntfy.sh/replace-with-your-topic
